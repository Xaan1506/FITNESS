{
  "name": "fittrack-fullstack",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && node server.js",
    "postinstall": "cd backend && npm install && npm rebuild better-sqlite3 --build-from-source"
  }
}
